name: Bid Scanner (Weekday 8AM JST)

on:
  schedule:
    - cron: '0 23 * * 0-4'  # 23:00 UTC Sun-Thu = 08:00 JST Mon-Fri
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Restore bid scanner state
        uses: actions/cache@v4
        with:
          path: scripts/bid_scanner_state.json
          key: bid-scanner-state-${{ github.run_id }}
          restore-keys: |
            bid-scanner-state-

      - name: Setup config
        env:
          LARK_APP_ID: ${{ secrets.LARK_APP_ID }}
          LARK_APP_SECRET: ${{ secrets.LARK_APP_SECRET }}
        run: python scripts/setup_config.py

      - name: Scan bids
        run: python scripts/bid_scanner.py --days 5
